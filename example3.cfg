\ Настройки облачного кластера

set BASE_PORT = 8000;
set OFFSET = 17;

([
  SERVER : ([
      HOST : 127,
      PORT : | BASE_PORT OFFSET + |
  ]),
  NODES : '(
     ([
        ID : 1,
        LOAD : | 10 3 mod() |
     ])
     ([
        ID : 2,
        LOAD : 4
     ])
     ([
        ID : 3,
        LOAD : | 7 2 mod() |
     ])
  ),
  LIMITS : ([
      CPU : 16,
      RAM : 64,
      THREADS : | 8 2 + |
  ])
])
