\ Конфигурация логирования

set BASE_LEVEL = 2;
set INCREMENT = 3;

([
  LOGGING : ([
      LEVEL : | BASE_LEVEL INCREMENT + |,
      MODULES : '( 1 2 3 4 ),
      ROTATION : ([
          SIZE : 1000,
          COUNT : 5
      ])
  ])
])
